// Allows us to use ES6 in our migrations and tests.
require('babel-register');
const Web3 = require('web3');
const HDWalletProvider = require('truffle-hdwallet-provider');
const path = require('path');

const mnemonic = 'ROPSTEN_MNEMONIC';

const buildDirConfig = require('./build_dir.json');

module.exports = {
  contracts_build_directory: path.join(__dirname, buildDirConfig.base_build_dir, 'contracts'),
  networks: {
    development: {
      gas: 6721975,
      gasPrice: 0,
      from: 'your address',
      host: 'localhost',
      port: 4444,
      network_id: '31' // Match any network id
    },
    ropsten: {
      provider: function() {
        // HDWalletProvider returns only 1 address by default. Set 4th parameter as number of addresses in constructor below.
        return new HDWalletProvider(mnemonic, 'https://ropsten.infura.io/ROPSTEN_INFURA_ID')
      },
      network_id: '3',
      gas: 4500000,
      gasPrice: 25000000000
    },
    test: {
      gas: 6721975,
      gasPrice: 0,
      host: 'localhost',
      port: 8545,
      network_id: '*' // Match any network id
    }
  },
  solc: {
    optimizer: {
      enabled: true,
      runs: 200
    }
  }
}
